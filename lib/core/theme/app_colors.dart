import 'package:flutter/material.dart';

class AppColors {
  // Màu chủ đạo (Primary) - Sky 500
  static const Color primary = Color(0xFF0EA5E9);

  // Màu Gradient
  static const Color gradientStart = Color(0xFF38BDF8); // sky-400
  static const Color gradientEnd = Color(0xFF0284C7);   // sky-600

  // Màu nền, chữ
  static const Color background = Colors.white;
  static const Color inputBackground = Color(0xFFF0F9FF); // sky-50
  static const Color textPrimary = Color(0xFF1E293B); // slate-800
  static const Color textSecondary = Colors.grey;
}